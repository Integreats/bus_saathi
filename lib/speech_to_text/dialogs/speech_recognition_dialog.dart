import 'package:flutter/material.dart';

class SpeechRecognitionText extends StatelessWidget {
  const SpeechRecognitionText({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}