import 'package:flutter/material.dart';

class BusReviewScreen extends StatelessWidget {
  const BusReviewScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      
    );
  }
}